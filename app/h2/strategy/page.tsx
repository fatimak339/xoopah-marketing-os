import PlaceholderPage from "@/components/PlaceholderPage";

export default function H2StrategyPage() {
  return (
    <PlaceholderPage
      eyebrow="H2 2026 / Strategy"
      title="Not started yet"
      description="H2 strategy work will land here once it's underway."
      crumbs={[{ label: "H2", href: "/h2" }]}
    />
  );
}
