import PlaceholderPage from "@/components/PlaceholderPage";

export default function H2ReportingPage() {
  return (
    <PlaceholderPage
      eyebrow="H2 2026 / Reporting"
      title="Not started yet"
      description="H2 reporting cadence and dashboards will land here once they're set up."
      crumbs={[{ label: "H2", href: "/h2" }]}
    />
  );
}
